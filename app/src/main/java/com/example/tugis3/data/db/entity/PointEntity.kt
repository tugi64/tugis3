package com.example.tugis3.data.db.entity

// Bu dosya önce app modülünde mükerrer PointEntity tanımı içeriyordu.
// Artık core/data içindeki PointEntity kullanılacak (index ve güncel alanlar orada).
// Çakışmayı önlemek için sınıf kaldırıldı.
// (Bilerek boş bırakıldı)
