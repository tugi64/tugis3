package com.example.tugis3.data.db.entity

// Bu dosya önce app modülünde mükerrer ProjectEntity tanımı içeriyordu.
// Asıl kaynak artık core/data modülündeki `com.example.tugis3.data.db.entity.ProjectEntity`.
// Çakışmayı önlemek için sınıf kaldırıldı. Yeni kod importlarını kontrol edin.
// (Boş bırak intentionally)
