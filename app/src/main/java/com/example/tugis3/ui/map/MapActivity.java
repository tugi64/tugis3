/*
 This Java MapActivity was replaced by a Compose-based MapActivity (MapActivity.kt).
 Keeping this file as a stub to avoid accidental reuse. The real MapActivity implementation
 lives in MapActivity.kt (Compose + AndroidView WebView integration).
*/
package com.example.tugis3.ui.map;

// This file intentionally left blank to avoid duplicate class definitions.
